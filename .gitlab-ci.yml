image: node:latest

cache:
  paths:
    - node_modules/

build:
  stage: build
  script:
    - npm install -g grunt-cli
    - npm install
    - grunt build

test:
  stage: test
  script:
    - npm install -g grunt-cli
    - grunt test
